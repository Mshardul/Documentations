# Wiki App — Backlog Task Board

| ID       | Entry Date | Summary                                          | Description                                                                                                                     | Status  | Impl. Date | Remarks                                                                            | priority | effort required |
| -------- | ---------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- | ------- | ---------- | ---------------------------------------------------------------------------------- | -------- | --------------- |
| WIKI-007 | 2026-04-21 | Smarter related articles ranking                 | Replace first-3-in-section logic with keyword or tag-based relevance ranking.                                                   | Backlog | —          |                                                                                    | p2       |
| WIKI-016 | 2026-04-19 | Prerequisites rendered as chips in content view  | Article prerequisites render as clickable chips in the content header, opening linked pages in-app.                             | Backlog | —          |                                                                                    | p0       | Medium          |
| WIKI-017 | 2026-04-19 | TLDR hover preview on article links              | Hovering an article link shows its TLDR section as a tooltip/popup.                                                             | Backlog | —          | Scoped as post-MVP during initial wiki design discussion.                          | p0       | Medium          |
| WIKI-021 | 2026-04-21 | Help page                                        | Dedicated page listing all keyboard shortcuts and usage tips. Accessible via `?` hotkey and settings link.                      | Backlog | —          | Low priority until hotkey list is stable.                                          | p2       |
| WIKI-026 | 2026-04-21 | Read progress badge on index hero                | Show "X / Y articles read" in index hero. Compute from `isRead()` + available (non-stub) cards.                                 | Backlog | —          |                                                                                    | p2       |
| WIKI-027 | 2026-04-21 | Related articles from in-article recommendations | Replace first-3-in-section with a `## Recommended` section in each `.md` file; parser feeds the related panel.                  | Backlog | —          | Requires adding `## Recommended` to existing content files. Surface non-stub only. | p1       |
| WIKI-030 | 2026-04-21 | Print / PDF export                               | `window.print()` from content topbar button. Print stylesheet hides sidebar, topbar, progress bar, and related articles.        | Backlog | —          |                                                                                    | p3       |
| WIKI-034 | 2026-04-22 | In-article search                                | `/` opens inline search bar highlighting all matches in current article. `n`/`N` jump between matches; Escape dismisses.        | Backlog | —          |                                                                                    | p2       |
| WIKI-036 | 2026-04-22 | Show unread only filter on index                 | Toggle on wiki index that hides already-read cards. State not persisted — resets each visit.                                    | Backlog | —          |                                                                                    | p2       |
| WIKI-037 | 2026-04-22 | Collapsible sections on index                    | Each section toggled open/closed via click on header. All expanded by default. State persisted per wiki in localStorage.        | Backlog | —          |                                                                                    | p1       |
| WIKI-039 | 2026-04-22 | Mermaid diagram theme sync                       | `mermaid.initialize()` hardcodes dark vars; diagrams look wrong on Light, Amber CRT, Matrix, Terminal. Re-init on theme change. | Backlog | —          | No-op when no diagrams visible. Re-render only active content view.                | p1       |
| WIKI-040 | 2026-04-22 | Article title in sticky content topbar           | Content topbar has no article title. IntersectionObserver on `h1` — reveal title when `h1` scrolls out; clear on navigate.      | Backlog | —          | Title should be small and muted — not same visual weight as topbar buttons.        | p0       | Low             |
| WIKI-041 | 2026-04-22 | Section-scoped filter in ⌘K (`>` prefix)         | `> foo` in ⌘K filters by section name instead of article title. Shows non-stub articles in matching sections. No new fetches.   | Backlog | —          | `>` matches VS Code convention. Discoverability deferred to WIKI-021.              | p1       |
| WIKI-042 | 2026-04-22 | Global bookmarks modal (⌘B)                      | ⌘B opens modal listing all bookmarks across wikis grouped by wiki. Inline filter, empty state, Escape/backdrop to close.        | Backlog | —          | Distinct from ⌘K — curated list vs. open search.                                   | p1       |
| WIKI-043 | 2026-04-22 | Article keyboard shortcuts (`b`, `,`)            | `b` toggles bookmark, `,` opens settings. Active in content view only when no modal open and focus not in input.                | Backlog | —          | `b` = bookmark (GitHub/Notion). `,` = settings (Slack/Linear). Docs via WIKI-021.  | p1       |
| WIKI-044 | 2026-04-22 | Inline image lightbox                            | Clicking any `<img>` in `.markdown-body` opens a full-screen overlay. Close on Escape or backdrop. No external lib.             | Backlog | —          | No-op on articles without images.                                                  | p2       |
| WIKI-045 | 2026-04-22 | Mermaid diagram zoom (click to fullscreen)       | Clicking `.mermaid-diagram` opens it fullscreen via shared `openLightbox()` helper from WIKI-044. SVG cloned; inline intact.    | Backlog | —          | Implement after WIKI-044. SVG clone must strip fixed width/height attrs.           | p1       |
| WIKI-046 | 2026-04-22 | Offline article list & bulk download             | Settings "Offline" section lists cached articles with remove buttons. Section header gets "Download all" for bulk caching.      | Backlog | —          | `cache.keys()` returns Request objects — URL normalisation needed to match paths.  | p2       |
| WIKI-047 | 2026-04-22 | Copy article as markdown                         | Topbar button copies raw `.md` source to clipboard. Store markdown on `state.currentMarkdown` in `renderContent` for reuse.     | Backlog | —          |                                                                                    | p3       |
| WIKI-048 | 2026-04-22 | Persist index scroll position per wiki           | Index scroll resets to top on back-navigate unlike article scroll. Save/restore under `index-scroll-${wikiId}` in localStorage. | Backlog | —          | Clear saved scroll on explicit home navigation.                                    | p1       |
| WIKI-049 | 2026-04-22 | Fix in-article link handling (external + anchor) | External links navigate away — add `target="_blank"`. Internal `#anchor` links don't scroll — intercept and `scrollIntoView`.   | Backlog | —          | `.md` links already handled. Covers the two remaining unhandled cases.             | p0       | Low             |
| WIKI-050 | 2026-04-22 | Home page glow and grid follow accent colour     | `.home-bg-grid` and `.home-glow` hardcode indigo. Replace with `var(--accent-dim)` and `var(--accent-glow)`.                    | Backlog | —          |                                                                                    | p0       |
| WIKI-051 | 2026-04-22 | OS theme detection on first visit                | First visit defaults to Dark regardless of OS. Apply Light/Dark via `prefers-color-scheme`. Priority: localStorage → OS → Dark. | Backlog | —          | Only fires when localStorage has no stored settings.                               | p0       | Trivial         |
| WIKI-052 | 2026-04-22 | Platform-aware keyboard shortcut hint            | Topbar shows `⌘K` on all platforms. Detect non-Mac via `navigator.platform` and show `Ctrl+K` instead.                          | Backlog | —          | Superseded by WIKI-021 — remove shortcut hint entirely when help page ships.       | p2       |
| WIKI-053 | 2026-04-22 | Table horizontal overflow visual cue             | Wide tables overflow with no cut-off signal. Wrap `<table>` in `div.table-scroll-wrap` with CSS `::after` fade on right edge.   | Backlog | —          | Fade uses `var(--bg)` → transparent to match any theme.                            | p2       |
| WIKI-054 | 2026-04-22 | Fix: `addToRecents` fires before fetch           | `addToRecents` called before `fetchText` — failed loads corrupt recents. Move inside `try` after fetch resolves.                | Backlog | —          |                                                                                    | p0       | Trivial         |
| WIKI-055 | 2026-04-22 | Fix: `populateIndexReadTimes` is sequential      | `populateIndexReadTimes` uses `for...of await` — one file at a time. Refactor to `Promise.all` like `loadAllSearchEntries`.     | Backlog | —          | Reuse `readTimeCache` to skip already-fetched files.                               | p0       | Low             |
| WIKI-056 | 2026-04-22 | Update `document.title` on navigation            | `document.title` never changes. Set to article name in content view, wiki name on index, default on home.                       | Backlog | —          |                                                                                    | p1       |
| WIKI-057 | 2026-04-22 | Service worker update notification               | New SW silently serves stale files. Show non-blocking toast on `updatefound` — "Update available, click to refresh".            | Backlog | —          | Toast must not auto-dismiss — user must choose to reload.                          | p4       |
| WIKI-058 | 2026-04-22 | Heading jump: brief highlight pulse on target    | TOC clicks give no visual landing confirmation. Flash `.heading-highlight` CSS animation on target `h2`/`h3` after scroll.      | Backlog | —          |                                                                                    | p1       |
